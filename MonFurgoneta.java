import java.util.ArrayList;

public class MonFurgoneta extends TipoMontaje {
    public MonFurgoneta(Vehiculo vActual, TipoMotor mActual, TipoTapiceria taActual, ArrayList<TipoRueda> rActuales) {
        super(vActual, mActual, taActual, rActuales);
    }
}