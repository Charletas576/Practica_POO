import java.util.ArrayList;

public class MonBiplaza extends TipoMontaje {
    public MonBiplaza(Vehiculo vActual, TipoMotor mActual, TipoTapiceria taActual, ArrayList<TipoRueda> rActuales) {
        super(vActual, mActual, taActual, rActuales);
    }
}